apiVersion: v1
kind: Service
metadata:
  labels:
    app: serializer
  name: serializer
  namespace: serializer
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 3000
  selector:
    app: serializer
  sessionAffinity: None
  type: ClusterIP
